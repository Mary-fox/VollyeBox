import './PopupAccount.scss';


function PopupAccount(props) {
    const { isPopupAccountOpen} = props;
 
    return (
    <div className={`popup-account ${isOfferOpen ? 'offer--open' : ''}`} >
        
        
    </div>

    );
  }
  
  export default PopupAccountr;
  

